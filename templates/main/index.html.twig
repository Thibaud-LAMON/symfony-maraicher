{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
    <div class="content">
        <h1 class="title">Bienvenue chez le maraicher</h1>
        <br>
        {% for entry in address%}
            <p>Nos horaires d'ouverture :</p>
            <p>{{ entry.address }}</p>
        {% endfor %}
        {% for entry in telephone%}
            <p>Nous contacter :</p>
            <p>{{ entry.telephone }}</p>
        {% endfor %}
        {% for entry in timetable%}
            <p>Nos horaires d'ouverture :</p>
            <p>{{ entry.timetable }}</p>
        {% endfor %}
    </div>

    <div>
        <a href="{{ path('app_products') }}" >
            <button>Passer commande</button>
        </a>
    </div>
{% endblock %}
